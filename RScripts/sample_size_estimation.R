# Two-sample t test power calculation

power.t.test(delta = 15, sd = 15, sig.level = 0.05, power = 0.8)
# delta = unterschied zwischen den zwei gruppen
# es wird die benötigte Anzahl an Proben berechnet
